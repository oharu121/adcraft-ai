/**
 * Zara - Video Producer Agent Mock Data (DEFERRED)
 *
 * IMPLEMENTATION STATUS: DEFERRED FOR FUTURE DEVELOPMENT
 */

// DEFERRED: Mock video production data
export const mockVideoProductionData = {
  // This will contain realistic demo data for Zara agent
  // when implementation begins
};

// DEFERRED: Mock handoff data from David
export const mockDavidHandoffData = {
  // This will contain sample creative handoff data
  // when implementation begins
};

// DEFERRED: Mock video generation responses
export const mockVideoResponses = {
  // This will contain sample video generation results
  // when implementation begins
};
